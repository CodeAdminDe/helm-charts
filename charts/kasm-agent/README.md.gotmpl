{{- define "kasm-agent.atom.helm.namespace" -}}
_**Note**: If you want to specify a namespace, provide the name via the `-n` (or `--namespace`) flag. You need to create the desired namespace beforehand._
{{- end -}}

{{- define "kasm-agent.atom.tldr" -}}
## TL;DR

You don't want to read through the docs? That's the quick and dirty way:

```bash
helm repo add {{ template "extra.atom.repository.owner" . }} {{ template "extra.atom.helm.url" . }}
helm repo update
helm install {{ template "chart.name" . }} {{ template "extra.atom.helm.path" . }}
```
{{ template "kasm-agent.atom.helm.namespace" . }}

> I'd recommend to read through the docs. Skip them at your own risk. ;-)
{{- end -}}

{{- define "kasm-agent.atom.install" -}}
## Installation

To install the chart with the release name `{{ template "chart.name" . }}`

```bash
helm install {{ template "chart.name" . }} {{ template "extra.atom.helm.path" . }}
```
{{ template "kasm-agent.atom.helm.namespace" . }}
{{- end -}}

{{- define "kasm-agent.atom.uninstall" -}}
## Uninstallation

To uninstall the chart release named `{{ template "chart.name" . }}`

```bash
helm uninstall {{ template "chart.name" . }}
```
{{ template "kasm-agent.atom.helm.namespace" . }}

The command removes all the Kubernetes components associated with the chart **including persistent volumes** and deletes the release.
{{- end -}}

{{- define "kasm-agent.atom.configsection" -}}
## Configuration

Take a look at the [values.yaml](./values.yaml) file of the chart. It contains the default configuration and additional notes.
So it would be a good idea to take a look at it, even if you already know what you would like to accomplish.

To configure the release (chart values), provide your own values via a *.yaml file and reference it with the `-f` flag.

> #### Sample
>
> ```bash
> helm install {{ template "chart.name" . }} {{ template "extra.atom.helm.path" . }} -f values.yaml
> ```

Alternatively, you could provide the values which you want to override at the CLI directly. Then you have to provide the values as key=value pair(s), referenced by the `--set` flag.

> #### Sample
>
> ```bash
> helm install {{ template "chart.name" . }} --set key=value --set keyTwo=secondValue {{ template "extra.atom.helm.path" . }}
> ```
{{- end -}}

{{- define "kasm-agent.atom.experimental-cilium-networkpolicies-provided" -}}
## Chart with experimental support for CiliumNetworkPolicies

Please note that this chart does not provide any production ready network policies itself.
Therefore, I recommend the implementation of network policies before using in prod environments.

> **If you're using Cilium CNI**: I've added experimental support for CNPs.
> Note that these are highly opinionated and you should review them carefully before using.
> E.g. it's required that each release gets deployed within a separate namespace.
{{- end -}}

{{- define "kasm-agent.templates.parts.main" -}}
{{ template "chart.description" . }}
{{ template "chart.homepageLine" . }}
{{ template "chart.maintainersSection" . }}
{{ template "chart.sourcesSection" . }}
{{ template "chart.requirementsSection" . }}
{{ template "kasm-agent.atom.tldr" . }}
{{ template "extra.atom.repo" . }}
{{ template "kasm-agent.atom.install" . }}
{{ template "kasm-agent.atom.uninstall" . }}
{{ template "kasm-agent.atom.configsection" . }}
{{- end -}}

{{- define "kasm-agent.templates.parts.footer" -}}
{{- $hasLibchartCnps := false }}
{{- range .Values }}
{{- if (eq .Key "libchartCnps") }}
{{- $hasLibchartCnps = true }}
{{- end }}
{{- end }}

{{- if (eq true $hasLibchartCnps) }}
{{ template "kasm-agent.atom.experimental-cilium-networkpolicies-provided" . }}
{{- else }}
{{ template "extra.atom.no-networkpolicies-provided" . }}
{{- end }}

{{ template "extra.atom.opinionated" . }}
{{ template "extra.atom.feedbackandsecurity" . }}

----------------------------------------------
Autogenerated from chart metadata using [helm-docs](https://github.com/norwoodj/helm-docs)
{{- end -}}

{{- define "kasm-agent.templates.readme.complexvalues" -}}
{{ template "extra.templates.parts.header" . }}
{{ template "kasm-agent.templates.parts.main" . }}
{{ template "chart.valuesSectionHtml" . }}
{{ template "extra.atom.valuetypesection" . }}
{{ template "kasm-agent.templates.parts.footer" . }}
{{- end -}}

{{- define "extra.specifics.fromchart.atom.opinionated" -}}
<hr /><hr />

## Appendix - Requirements and Scope

### Required Secret: Manager Token

This chart expects an existing Kubernetes secret for the manager token.

Sample:

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: kasm-agent-manager-token
type: Opaque
stringData:
  manager-token: "CHANGE_ME__I_AM_A_SAMPLE"
```

> **IMPORTANT**: Never commit unencrypted secrets into Git repositories.

### Vendor Scope Note

According to Kasm vendor documentation, static Docker agents are not part of the official Kubernetes technical-preview deployment model.
This chart is therefore intended as an advanced, environment-specific option and should be validated carefully in your platform before production usage.

{{- end -}}

{{ template "kasm-agent.templates.readme.complexvalues" . }}
{{ template "extra.specifics.fromchart.atom.opinionated" . }}
