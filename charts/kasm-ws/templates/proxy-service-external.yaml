{{/*
Deploy the Kasm Proxy service
*/}}
{{- if ne .Values.proxyService.type "ClusterIP" -}}
  {{- fail "Only proxyService.type=ClusterIP is supported in this chart. Expose traffic through ingress/route resources." -}}
{{- end -}}
