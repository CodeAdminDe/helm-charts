1. Access LocalRecall inside the cluster:
   kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "local-recall.fullname" . }} 8080:{{ .Values.service.port }}

2. If ingress is enabled, use one of the configured hosts:
   {{- if .Values.ingress.enabled }}
   {{- range .Values.ingress.hosts }}
   - {{ .host }}
   {{- end }}
   {{- else }}
   ingress disabled
   {{- end }}

3. If Gateway API is enabled, verify HTTPRoute admission:
   {{- if .Values.gatewayApi.enabled }}
   kubectl -n {{ .Release.Namespace }} get httproute {{ include "local-recall.httpRouteName" . }}
   {{- else }}
   gatewayApi disabled
   {{- end }}
