{{- if ne .Values.service.type "ClusterIP" -}}
{{- fail "service.type must be ClusterIP in this repo style. Expose externally via ingress/route resources." -}}
{{- end -}}

{{- if and .Values.ingress.enabled (not .Values.ingress.hosts) -}}
{{- fail "ingress.enabled=true requires at least one ingress.hosts entry." -}}
{{- end -}}

{{- if .Values.gatewayApi.enabled -}}
  {{- if not .Values.gatewayApi.parentRefs -}}
    {{- fail "gatewayApi.enabled=true requires at least one gatewayApi.parentRefs entry." -}}
  {{- end -}}
  {{- if not .Values.gatewayApi.hostnames -}}
    {{- fail "gatewayApi.enabled=true requires at least one gatewayApi.hostnames entry." -}}
  {{- end -}}
{{- end -}}

{{- if and .Values.rbac.create (not .Values.serviceAccount.create) (empty .Values.serviceAccount.name) -}}
{{- fail "rbac.create=true requires serviceAccount.create=true or a non-empty serviceAccount.name." -}}
{{- end -}}

{{- if and .Values.rbac.create (empty .Values.rbac.rules) -}}
{{- fail "rbac.create=true requires explicit rbac.rules; no broad default Role is generated by this chart." -}}
{{- end -}}

{{- if and .Values.libchartCnps.includeCnpgPolicies (not .Values.libchartCnps.enabled) -}}
{{- fail "libchartCnps.includeCnpgPolicies=true requires libchartCnps.enabled=true." -}}
{{- end -}}

{{- if and (not .Values.persistence.enabled) (not (empty .Values.persistence.existingClaim)) -}}
{{- fail "persistence.existingClaim requires persistence.enabled=true." -}}
{{- end -}}

{{- if and .Values.securityContext.readOnlyRootFilesystem (not .Values.tmpVolume.enabled) -}}
{{- fail "securityContext.readOnlyRootFilesystem=true requires tmpVolume.enabled=true for upload/temp file handling." -}}
{{- end -}}

{{- if not (has (default "chromem" .Values.localRecall.vectorEngine) (list "chromem" "postgres" "localai")) -}}
{{- fail "localRecall.vectorEngine must be one of: chromem, postgres, localai." -}}
{{- end -}}

{{- if and .Values.localRecall.openAIApiKeySecret.name (empty .Values.localRecall.openAIApiKeySecret.key) -}}
{{- fail "localRecall.openAIApiKeySecret.key must be set when localRecall.openAIApiKeySecret.name is provided." -}}
{{- end -}}

{{- if and .Values.localRecall.apiKeysSecret.name (empty .Values.localRecall.apiKeysSecret.key) -}}
{{- fail "localRecall.apiKeysSecret.key must be set when localRecall.apiKeysSecret.name is provided." -}}
{{- end -}}

{{- if and .Values.localRecall.gitPrivateKeySecret.name (empty .Values.localRecall.gitPrivateKeySecret.key) -}}
{{- fail "localRecall.gitPrivateKeySecret.key must be set when localRecall.gitPrivateKeySecret.name is provided." -}}
{{- end -}}

{{- if not (hasPrefix .Values.persistence.mountPath .Values.localRecall.collectionDbPath) -}}
{{- fail "localRecall.collectionDbPath must be inside persistence.mountPath." -}}
{{- end -}}

{{- if not (hasPrefix .Values.persistence.mountPath .Values.localRecall.fileAssets) -}}
{{- fail "localRecall.fileAssets must be inside persistence.mountPath." -}}
{{- end -}}

{{- if eq .Values.localRecall.vectorEngine "postgres" -}}
  {{- if empty .Values.database.postgres.ssl -}}
    {{- fail "database.postgres.ssl must be set when localRecall.vectorEngine=postgres." -}}
  {{- end -}}
  {{- if .Values.database.useCnpgCluster.enabled -}}
    {{- if empty .Values.database.useCnpgCluster.clusterName -}}
      {{- fail "database.useCnpgCluster.clusterName must be set when CNPG integration is enabled." -}}
    {{- end -}}
    {{- if empty .Values.database.useCnpgCluster.secretKeys.database -}}
      {{- fail "database.useCnpgCluster.secretKeys.database is required when CNPG integration is enabled." -}}
    {{- end -}}
    {{- if empty .Values.database.useCnpgCluster.secretKeys.username -}}
      {{- fail "database.useCnpgCluster.secretKeys.username is required when CNPG integration is enabled." -}}
    {{- end -}}
    {{- if empty .Values.database.useCnpgCluster.secretKeys.password -}}
      {{- fail "database.useCnpgCluster.secretKeys.password is required when CNPG integration is enabled." -}}
    {{- end -}}
  {{- else -}}
    {{- if empty .Values.database.postgres.host -}}
      {{- fail "database.postgres.host must be set when localRecall.vectorEngine=postgres and CNPG mode is disabled." -}}
    {{- end -}}
    {{- if empty .Values.database.postgres.database -}}
      {{- fail "database.postgres.database must be set when localRecall.vectorEngine=postgres and CNPG mode is disabled." -}}
    {{- end -}}
    {{- if empty .Values.database.postgres.user -}}
      {{- fail "database.postgres.user must be set when localRecall.vectorEngine=postgres and CNPG mode is disabled." -}}
    {{- end -}}
    {{- if empty .Values.database.postgres.passwordSecret.name -}}
      {{- fail "database.postgres.passwordSecret.name must be set when localRecall.vectorEngine=postgres and CNPG mode is disabled." -}}
    {{- end -}}
    {{- if empty .Values.database.postgres.passwordSecret.key -}}
      {{- fail "database.postgres.passwordSecret.key must be set when localRecall.vectorEngine=postgres and CNPG mode is disabled." -}}
    {{- end -}}
  {{- end -}}
{{- end -}}
