Welcome to {{ include "netbird-management.fullname" . }}!

{{- if eq .Values.mode.architecture "combined" }}
echo "* Check server pods via: kubectl get pods --namespace {{ .Release.Namespace }} -l app.kubernetes.io/component=server,app.kubernetes.io/name={{ include "netbird-management.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"
echo "* Check logs via: kubectl logs --namespace {{ .Release.Namespace }} deployment/{{ include "netbird-management.fullname" . }}-server"
{{- else }}
echo "* Check management pods via: kubectl get pods --namespace {{ .Release.Namespace }} -l app.kubernetes.io/component=management,app.kubernetes.io/name={{ include "netbird-management.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"
echo "* Check logs via: kubectl logs --namespace {{ .Release.Namespace }} deployment/{{ include "netbird-management.fullname" . }}-management"
{{- end }}

echo "Happy networking!"
