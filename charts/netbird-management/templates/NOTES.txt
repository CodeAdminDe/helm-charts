Welcome to {{ include "netbird-management.fullname" . }}!

{{- if eq .Values.mode.architecture "combined" }}
export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/component=server,app.kubernetes.io/name={{ include "netbird-management.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
echo "* Check the status of the server via: kubectl get --namespace {{ .Release.Namespace }} pod/$POD_NAME"
echo "* Check the logs of the server via: kubectl logs --namespace {{ .Release.Namespace }} $POD_NAME"
{{- else }}
export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/component=management,app.kubernetes.io/name={{ include "netbird-management.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
echo "* Check the status of the management service via: kubectl get --namespace {{ .Release.Namespace }} pod/$POD_NAME"
echo "* Check the logs of the management service via: kubectl logs --namespace {{ .Release.Namespace }} $POD_NAME"
{{- end }}

echo "Happy networking!"
