apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coder.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "coder.labels" . | nindent 4 }}
    {{- include "coder.componentLabels" (dict "component" "test") | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  {{- if .Values.runtimeClass.tests }}
  runtimeClassName: {{ .Values.runtimeClass.tests }}
  {{- end }}
  containers:
    - name: wget
      image: busybox:1.37
      command: ["sh", "-c"]
      args:
        - |
          wget -qO- http://{{ include "coder.fullname" . }}:{{ .Values.service.port }}/healthz
  restartPolicy: Never
