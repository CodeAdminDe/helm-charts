{{- if and .Values.documentConversion.enabled .Values.gotenberg.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "paperless-ngx.gotenbergServiceName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "paperless-ngx.labels" . | nindent 4 }}
    {{- include "paperless-ngx.componentLabels" (dict "root" . "component" "gotenberg") | nindent 4 }}
    {{- with .Values.gotenberg.service.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.gotenberg.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.gotenberg.service.port }}
      targetPort: {{ .Values.gotenberg.service.targetPort }}
      protocol: {{ .Values.gotenberg.service.targetProtocol }}
      name: http
  selector:
    {{- include "paperless-ngx.componentSelectorLabels" (dict "root" . "component" "gotenberg") | nindent 4 }}
{{- end }}
