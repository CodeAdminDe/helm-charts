apiVersion: v1
kind: Pod
metadata:
  name: '{{ include "anything-llm.fullname" . }}-test-connection'
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "anything-llm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  activeDeadlineSeconds: 60
  automountServiceAccountToken: false
  {{- if .Values.runtimeClass.tests }}
  runtimeClassName: {{ .Values.runtimeClass.tests }}
  {{- end }}
  securityContext:
    runAsNonRoot: true
    runAsUser: 65534
    seccompProfile:
      type: RuntimeDefault
  containers:
    - name: wget
      image: busybox:1.37
      imagePullPolicy: IfNotPresent
      command: ["wget"]
      args: ["--spider", "--timeout=10", "--tries=3", 'http://{{ include "anything-llm.fullname" . }}:{{ .Values.service.port }}/api/ping']
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        allowPrivilegeEscalation: false
        readOnlyRootFilesystem: true
        seccompProfile:
          type: RuntimeDefault
        capabilities:
          drop:
            - ALL
  restartPolicy: Never
