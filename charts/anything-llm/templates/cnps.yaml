{{- define "anything-llm.CiliumNetworkPolicies-NamespaceSet" -}}
metadata:
  namespace: {{ .Release.Namespace }}
  annotations:
    checksum/values: {{ toJson .Values | sha256sum }}
  labels:
    {{- include "anything-llm.labels" . | nindent 4 }}
{{- end -}}

{{- if .Values.libchartCnps.enabled -}}
{{ include "libchart-cnps.CiliumNetworkPolicies-NamespaceSet" (list . "anything-llm.CiliumNetworkPolicies-NamespaceSet") }}
---
{{- end -}}

{{- define "anything-llm.CiliumNetworkPolicies-CnpgSet" -}}
metadata:
  namespace: {{ .Release.Namespace }}
  annotations:
    checksum/values: {{ toJson .Values | sha256sum }}
  labels:
    {{- include "anything-llm.labels" . | nindent 4 }}
{{- end -}}

{{- if and .Values.libchartCnps.enabled .Values.libchartCnps.includeCnpgPolicies .Values.database.useCnpgCluster.enabled -}}
{{ include "libchart-cnps.CiliumNetworkPolicies-CnpgSet" (list . "anything-llm.CiliumNetworkPolicies-CnpgSet") }}
---
{{- end -}}

{{- define "anything-llm.CiliumNetworkPolicies-AppSet" -}}
{{- $defaultIngressEndpoints := list -}}
{{- if .Values.ingress.enabled -}}
{{- $defaultIngressEndpoints = append $defaultIngressEndpoints (dict "app.kubernetes.io/name" "ingress-nginx" "io.kubernetes.pod.namespace" "ingress-nginx") -}}
{{- end -}}
{{- if .Values.gatewayApi.enabled -}}
{{- if .Values.gatewayApi.controllerSelector -}}
{{- $defaultIngressEndpoints = append $defaultIngressEndpoints .Values.gatewayApi.controllerSelector -}}
{{- end -}}
{{- end -}}
{{- $defaultDenyMatchLabel := dict "anything-llm.cnp/default-deny" "true" -}}
{{- $effectiveIngressEndpoints := .Values.cnps.appTraffic.ingress.fromEndpoints | default (list) -}}
{{- $effectiveIngressEntities := .Values.cnps.appTraffic.ingress.fromEntities | default (list) -}}
{{- if and (empty $effectiveIngressEndpoints) (empty $effectiveIngressEntities) (gt (len $defaultIngressEndpoints) 0) -}}
{{- $effectiveIngressEndpoints = $defaultIngressEndpoints -}}
{{- end -}}
{{- if not .Values.cnps.appTraffic.ingress.allow -}}
{{- $effectiveIngressEndpoints = list $defaultDenyMatchLabel -}}
{{- $effectiveIngressEntities = list -}}
{{- end -}}
{{- $hasIngressEndpoints := gt (len $effectiveIngressEndpoints) 0 -}}
{{- $hasIngressEntities := not (empty $effectiveIngressEntities) -}}
{{- if and .Values.cnps.appTraffic.ingress.allow (not $hasIngressEndpoints) (not $hasIngressEntities) -}}
{{- $effectiveIngressEndpoints = list $defaultDenyMatchLabel -}}
{{- $hasIngressEndpoints = true -}}
{{- end -}}
{{- $includeIngressRule := or $hasIngressEndpoints $hasIngressEntities -}}
{{- $effectiveEgressPorts := .Values.cnps.appTraffic.egress.toPorts | default (list) -}}
{{- if eq (default "sqlite" .Values.database.mode) "postgres" -}}
{{- $dbPort := printf "%v" .Values.database.postgres.port -}}
{{- if .Values.database.useCnpgCluster.enabled -}}
{{- $dbPort = printf "%v" .Values.database.useCnpgCluster.port -}}
{{- end -}}
{{- $effectiveEgressPorts = concat $effectiveEgressPorts (list (dict "port" $dbPort "protocol" "TCP")) -}}
{{- end -}}
metadata:
  namespace: {{ .Release.Namespace }}
  annotations:
    checksum/values: {{ toJson .Values | sha256sum }}
  labels:
    {{- include "anything-llm.labels" . | nindent 4 }}
spec:
  endpointSelector:
    matchLabels:
      {{- include "anything-llm.selectorLabels" . | nindent 6 }}
  {{- if $includeIngressRule }}
  ingress:
    - {{- if $hasIngressEndpoints }}
      fromEndpoints:
        {{- range $effectiveIngressEndpoints }}
        - matchLabels:
          {{- range $k, $v := . }}
            {{ $k }}: {{ $v | quote }}
          {{- end }}
        {{- end }}
      {{- end }}
      {{- if $hasIngressEntities }}
      fromEntities:
        {{- toYaml $effectiveIngressEntities | nindent 8 }}
      {{- end }}
      toPorts:
        - ports:
            - port: {{ printf "%v" .Values.service.targetPort | quote }}
              protocol: {{ .Values.service.targetProtocol | default "TCP" }}
  {{- else }}
  ingress: []
  {{- end }}
  {{- if .Values.cnps.appTraffic.egress.allow }}
  egress:
    - toEntities:
      {{- if .Values.cnps.appTraffic.egress.toEntities }}
        {{- toYaml .Values.cnps.appTraffic.egress.toEntities | nindent 8 }}
      {{- else }}
        - world
      {{- end }}
      toPorts:
        - ports:
          {{- if $effectiveEgressPorts }}
          {{- range $effectiveEgressPorts }}
            - port: {{ .port | quote }}
              {{- if hasKey . "endPort" }}
              endPort: {{ .endPort }}
              {{- end }}
              protocol: {{ .protocol | default "TCP" }}
          {{- end }}
          {{- else }}
            - port: "443"
              protocol: TCP
          {{- end }}
  {{- else }}
  egress:
    - toEndpoints:
        - matchLabels:
            anything-llm.cnp/default-deny: "true"
      toPorts:
        - ports:
            - port: "65535"
              protocol: TCP
  {{- end }}
{{- end -}}

{{- if .Values.libchartCnps.enabled -}}
{{ include "libchart-cnps.CiliumNetworkPolicies-AppSet" (list . "anything-llm.CiliumNetworkPolicies-AppSet") }}
{{- end -}}
