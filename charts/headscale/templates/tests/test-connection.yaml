apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "headscale.fullname" . }}-test-connection"
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "headscale.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  {{- if .Values.runtimeClass.tests }}
  runtimeClassName: {{ .Values.runtimeClass.tests }}
  {{- end }}
  containers:
    - name: wget
      image: busybox:1.37
      command: ["sh", "-c"]
      args:
        - |
          wget -qO- http://{{ include "headscale.fullname" . }}:{{ .Values.service.http.port }}/health
  restartPolicy: Never
