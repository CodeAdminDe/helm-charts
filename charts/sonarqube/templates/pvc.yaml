{{- if and .Values.persistence.enabled (not .Values.persistence.existingClaim) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "sonarqube.persistenceClaimName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sonarqube.labels" . | nindent 4 }}
  {{- if or .Values.persistence.retain .Values.persistence.annotations }}
  annotations:
    {{- if .Values.persistence.retain }}
    helm.sh/resource-policy: keep
    {{- end }}
    {{- with .Values.persistence.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  accessModes:
    {{- toYaml .Values.persistence.accessModes | nindent 4 }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}
  {{- with .Values.persistence.storageClass }}
  storageClassName: {{ . }}
  {{- end }}
  {{- with .Values.persistence.volumeName }}
  volumeName: {{ . }}
  {{- end }}
{{- end }}
{{- if and .Values.extensionsPersistence.enabled (not .Values.extensionsPersistence.existingClaim) }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "sonarqube.extensionsPersistenceClaimName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sonarqube.labels" . | nindent 4 }}
  {{- if or .Values.extensionsPersistence.retain .Values.extensionsPersistence.annotations }}
  annotations:
    {{- if .Values.extensionsPersistence.retain }}
    helm.sh/resource-policy: keep
    {{- end }}
    {{- with .Values.extensionsPersistence.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  accessModes:
    {{- toYaml .Values.extensionsPersistence.accessModes | nindent 4 }}
  resources:
    requests:
      storage: {{ .Values.extensionsPersistence.size }}
  {{- with .Values.extensionsPersistence.storageClass }}
  storageClassName: {{ . }}
  {{- end }}
  {{- with .Values.extensionsPersistence.volumeName }}
  volumeName: {{ . }}
  {{- end }}
{{- end }}
