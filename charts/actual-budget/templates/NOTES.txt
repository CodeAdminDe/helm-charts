1. Access the application inside the cluster:
   kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "actual-budget.fullname" . }} 8080:{{ .Values.service.port }}

2. If ingress is enabled, use one of the configured hosts:
   {{- if .Values.ingress.enabled }}
   {{- range .Values.ingress.hosts }}
   - {{ .host }}
   {{- end }}
   {{- else }}
   ingress disabled
   {{- end }}

3. If Gateway API is enabled, verify HTTPRoute admission and use one of the configured hostnames:
   {{- if .Values.gatewayApi.enabled }}
   kubectl -n {{ .Release.Namespace }} get httproute {{ include "actual-budget.httpRouteName" . }}
   {{- range .Values.gatewayApi.hostnames }}
   - {{ . }}
   {{- end }}
   {{- else }}
   gatewayApi disabled
   {{- end }}

NOTE: Actual Budget requires HTTPS/TLS for many features (like web-app installation and secure syncing). Please ensure your Ingress or Gateway terminates TLS.
