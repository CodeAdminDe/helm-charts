{{- if .Values.rbac.create }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "garage.rbacRoleName" . }}
  namespace: {{ include "garage.rbacNamespace" . }}
  labels:
    {{- include "garage.labels" . | nindent 4 }}
rules:
  {{- toYaml .Values.rbac.rules | nindent 2 }}
{{- end }}
